// ** fetch users recurring transactions in the last 90 days
export const DEFAULT_RECURRING_TRANSACTION_LIMIT = 180;
export const API_BASE = "/api/v1";

export declare enum Products {
    Transactions = "transactions",
}